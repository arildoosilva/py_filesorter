"""
This is a simple script to sort media files and move them to their folders.

HOW TO USE:
1. change the source and destination strings.
    On Windows - C:/the/path/
    On Linux - /the/path/
2. Copy the if statement, changing the key and the folder.
3. Run
"""

import os
import shutil